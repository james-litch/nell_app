part of bottom_sheet_widget;

class _BottomSheetDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('bottom_sheet_desktop'),
    );
  }
}