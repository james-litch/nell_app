part of rounded_text_box_widget;

class _RoundedTextBoxDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('rounded_text_box_desktop'),
    );
  }
}