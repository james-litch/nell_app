part of rounded_button_widget;

class _RoundedButtonDesktop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('rounded_button_desktop'),
    );
  }
}